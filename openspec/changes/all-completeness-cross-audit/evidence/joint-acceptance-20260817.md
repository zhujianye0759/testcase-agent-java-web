# V2.0 联合验收记录（2026-08-17）

## 结论

本轮联合验收 **未通过完整生成门禁**，`6.3` 保持未完成，不得归档为已完成。

- 最终双向核对的 30 个功能审核批次全部通过。
- 冻结功能共 227 个，其中 175 个可生成、52 个明确不可生成。
- 三次有界生成后，170 个可生成批次接收，5 个批次失败；接收测试用例 340 条，期望为 350 条。
- 最终任务状态为 `PARTIAL`，覆盖状态为“审查完整但用例不完整”；最后 5 个批次中，2 个因执行步骤与预期结果编号不一一对应、2 个因通用经验内容未逐字等于固定文案、1 个因审查证据未保留当前冻结目标的 `candidateIds` 而耗尽三次。
- 系统未把部分结果标记为完整；生成的部分制品仅用于核对，不作为“完整且无遗漏”的验收交付。

## 重试与保护边界

- 第一次失败终态为 132/175 个批次接收、264 条用例；只重新领取 43 个失败批次，已接收批次未再次调用或覆盖。
- 第二次终态为 161/175 个批次接收、322 条用例；剩余 14 个失败均为通用经验固定文案与正式证据标记混写。
- 第三次只重新领取上述 14 个失败批次；最终 170/175 个批次接收、340 条用例，5 个批次耗尽最大三次，未创建第 4 次尝试。
- 对最后两个编号失败响应的受控结构诊断均显示步骤编号为 `1,2`、预期结果编号仅为 `1`；诊断未输出业务正文、候选编号或内部 ID。
- 首轮 ALL 提示及既有指定功能模式保持不变；重试只读取紧邻上一失败 attempt 的既有安全原因，并使用白名单固定纠错规则，未知原因不回灌原文。

## 已验证

- `WorkflowDiagnosticsTest` 4/4、`FeatureAuditServiceTest` 50/50、`GenerationWorkflowAllModeTest` 15/15，合计 69/69 通过。
- `openspec validate all-completeness-cross-audit --strict` 通过。
- `git diff --check` 通过，仅有既有 CRLF 提示。
- 部分制品具有精确两张合同 Sheet；测试用例 Sheet 为 1 行表头加 340 条数据。
- 任务详情、任务列表和工作簿均未出现 `documentId=`、`unitId=`、`candidateIds=`、`groupAnchorId=` 内部标记。
- Java Web 8082 与 KEE 8080 健康检查均返回 200。

## 未验证或未执行

- 因本轮禁止 Testcontainers，未运行 `MarkdownBatchPersistenceIntegrationTest`，不得宣称该测试通过。
- 未归档、未提交、未推送。
- 完整性门禁仍差 5 个可生成功能和 10 条测试用例；在用户确认新的处理方案前，不再修改尝试上限、数据库或校验器。
