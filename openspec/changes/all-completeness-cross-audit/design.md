## Context（上下文）

当前 `ALL` 路径仅向配置的 KEE 智能体请求一次两列表的 Markdown 功能清单，接收任意非空且序号为正整数并互不重复的集合，冻结这些行，并在所得批次均未失败时将任务标记为完成。因此，已完成的 V2.0 任务 `1d768085-af64-4c5f-9299-b27042249c44` 虽然所选功能清单包含远多于九条的源条目，且需求文档存在额外功能章节，仍接收了九个发现功能并导出了十八个测试用例。

一次只读可行性验证使用精确的 V2.0 范围，未改变任何 KEE 状态。对每个 `/knowledge/{id}/preview` 响应进行两次读取，结果字节完全一致。功能清单 preview 暴露了 1 至 128 的序号，但因序号 113 出现两次而得到 129 个记录形态匹配项。工单 preview 暴露了截至 `4.2.2.2.6.6.1` 的 100 个唯一编号章节。然而，`/preview` 是当前已启用 Chunk 的拼接结果，未暴露总单元数、游标或显式完成标记。它不能证明当前读取可见的全部已持久化解析文本 Chunk 已被遍历和审查，更不能证明原始文件的页、Sheet、行、单元格或章节坐标。已部署的 `/chunks/{id}` 路由对当前服务凭证返回 404，不能作为可用的回退契约。

此 change 涉及多个模块，但仍是一个编排 change。KEE 继续负责 Office/PDF 解析和源授权。Java Web 负责材料覆盖、审查收敛、任务耐久性、冻结生成范围和如实完成。模型生成内容仍为 Markdown。

## Goals / Non-Goals（目标/非目标）

**目标：**

- 在 `ALL` 生成前，使每一份当前最新、已授权源文档的已持久化文本 Chunk 枚举均可审计。
- 双向交叉审查功能清单候选项和需求候选项。
- 保留可追溯的遗漏、冲突、拆分、合并、重复和证据不足结论。
- 冻结稳定的最终功能集合，并为每个可生成的功能恰好接收一个正向和一个反向测试用例。
- 防止任何未读取、无法说明、未生成、被截断、遍历失败或格式错误的工作进入 `COMPLETED`。
- 保持浏览器流程简单，导出限制为现有两个业务 Sheet。

**非目标：**

- 恢复 `testcase-agent.response.v1`、`generation-result-1.0` 或其他任何模型 JSON 输出契约。
- 在 Java Web 中解析原始 Excel、PDF、Word、OCR 或图像。
- 将 parsed-units 的 Chunk 枚举完整性误称为原生解析保真度；后者另以代表性 PDF、DOCX 和 XLSX 样本验收。
- 为历史版本增加参数，或通过比较内容标识、摘要或自动替换侦测控制本次遍历。
- 修改 KEE 检索、排序、重排序、PageIndex、Wiki、Agent 循环、提示词路由或最终回答行为。
- 新增第三个工作簿 Sheet、审批流程、人工功能审查门禁、登录、角色或权限管理 UI。
- 声称语义模型判断在数学上绝对正确。此处的保证是：不完整处理不能被表示为完整。

## Decisions（决策）

### 1. 采用 KEE V1 parsed-units 合同，而不将 preview 视为清单 API

独立 KEE changes 已完成开发、review、定向测试、真实运行验收和回滚演练，但尚未合并、推送或归档；当前 KEE 已回滚旧镜像。因此，本 Java change 以确定性 WireMock fixture 实施消费者合同，真实联合验收时临时部署 KEE changes。读取端点固定为 `GET /api/v1/knowledge/{knowledge_id}/parsed-units`。它在每一页复用现有 API-key、tenant、KB、系统、版本、材料类型、可选项目和文档白名单约束，越界仍按既有 `forbidden` 失败关闭。它返回确定性的服务 JSON envelope；这不是模型响应契约。

响应 `data` 仅包含 `knowledge_id`、`total_units`、`units`、`next_cursor`、`complete`。每个 `unit` 仅包含 `unit_id`、`chunk_index`、`ordinal`、`content`、`start_at`、`end_at`。端点只读取当前最新、已授权文档已经持久化的 text Chunk，不接受历史版本、revision、hash 或 snapshot 参数，也不返回它们。正常请求分页默认 50 个单元，最大 100；请求超过 100 时按 100 处理。正常续页使用不透明 HMAC keyset `next_cursor`。每个完整 JSON 响应页最大 4 MiB；若单个单元本身超过该上限，服务返回 `unit_too_large`，并且绝不截断文本。显式业务错误包括 `document_not_ready`、`cursor_signing_unavailable`、`invalid_cursor`、`document_not_current`、`parsed_unit_integrity_error` 和 `unit_too_large`。`complete=true` 只证明本次读取可见的持久化 text Chunk 枚举完成；它不提供或保证 PDF/Word 页、Excel Sheet/行/单元格、原生结构或解析保真度。原生解析保真度另以代表性 PDF、DOCX 和 XLSX 样本验收。

上传与解析必须在调用前完成，且文件不在任务运行中主动替换。Java 必须从无游标的第一页开始，在每页范围和文档白名单校验通过后，验证 `knowledge_id` 一致、`unit_id` 全局唯一、`ordinal` 严格连续为 `1..total_units`、游标无循环或缺口。仅当累计单元数恰好等于 `total_units`，且最终页 `next_cursor=null`、`complete=true` 时，才可将“已遍历解析材料”门禁置绿并冻结材料清单；任一页 HTTP、JSON、范围、游标或单元完整性失败均使整次遍历失败。解析文本中展示的业务序号允许重复，Java 将其作为不同候选出现保留，而不把它误当作接口单元的唯一标识。

若调用方明确获知文件在本次遍历中被替换，Java 丢弃已接收的部分结果并从第一页重新开始。系统不比较内容标识，也不推断文件是否变化；没有该明确输入时，不产生替换结论。`/preview` 仍可用于展示和诊断，但不是完整性来源。

备选方案：在 Java 中解析 preview Markdown。已拒绝，因为 preview 无法证明源单元覆盖，且原始文档解析属于 KEE。备选方案：依赖 Agent `list_knowledge_chunks` 工具。已拒绝，因为工具调用由模型控制，chunk 覆盖不是耐久的外层循环保证。

### 2. 在语义审查前持久化源覆盖台账

应用数据库独立于模型输出保存源清单：

- `material_inventory_unit`：文档角色、既有 `chunk_id`、`ordinal`、文本和已接收处理状态；
- `feature_source_candidate`：从一个功能清单或需求解析单元提取出的候选项及其既有 KEE 文档/Chunk 证据引用；
- `feature_review_conclusion`：双向映射和终态审查结论；
- `frozen_feature_target`：最终功能标识、源候选项、拆分/合并原因、可生成性和稳定顺序；
- 用于可安全重启、幂等单元处理的审查工作项和尝试记录。

该台账只记录接收和处理状态，绝不成为第二套来源证据模型：正式业务证据始终使用既有 KEE 文档/Chunk 引用。冻结后，任务范围、提示词/Markdown 契约版本和冻结功能集合均不可变。调用方明确报告替换时，按本 change 的重启规则重新读取和审查；其他材料遍历错误使任务失败，而不是混合部分结果。

### 3. 扫描有界源单元，再执行双向审查

Java 控制处理哪个源单元，并为每个单元记录终态结果。它绝不发送无界的整份文档，也绝不要求一次调用证明其已读取全部内容。

功能清单扫描从每个解析单元中的功能条目产生候选项，保留重复展示的业务序号或文本出现而不按序号去重。需求扫描对每个稳定单元处理两次：第一遍提取动作/对象/条件候选项，第二遍检查同一解析单元中第一遍未覆盖的功能性陈述。未收敛的单元将在有界预算内重试，否则审查失败。

同一耐久扫描工作被重领时，认领事务只读取该工作最近一条已持久化失败摘要。它不是模型上下文的透传通道：第二次和第三次均附加同一份固定、全面的 Markdown 格式基线，以免模型在标题、HTML、列数和坐标之间来回摆动；基线要求输出首字符为 `#`、首行精确为第一张表标题且其前没有分析、说明、结论或引导语，并且两张表连续出现，第一表后不得插入思考、自我复核、撤销、重复标题或重复表。只有白名单中的严格合同错误才额外转换为固定重点提示。基线只能以自然语言指向本次提示已提供的坐标标记，不含占位符或动态坐标值。首次没有该提示，未知、空或缺失摘要只使用全面基线。任何原始异常、URL、路径、凭据或机器标识均不注入提示词。该反馈不改变扫描器、最终双向核对、三次上限、失败关闭或不可变台账。

源扫描后，Agent 提出语义关系，Java 持久化并校验其覆盖：

- 匹配；
- 对仅存在于需求中的功能标记 `功能清单遗漏`；
- 对仅存在于功能清单中的功能标记 `需求未覆盖该功能点`；
- 描述冲突；
- 粒度不匹配和拆分；
- 重复和合并；
- 证据不足且不可生成。

每个源候选项必须具有恰好一个终态处置，并且必须映射到冻结功能或已记录的不可生成结论。完全相同的源重复项仍作为发现可见；语义合并建议不得消除来源追溯信息。

最终双向核对不得把全部候选的完整结论一次性要求模型返回：真实全量材料可超过模型稳定输出上限。Java 每次仍发送全量候选作为唯一的比较上下文，但每个功能审核批次至多选择 8 个原始候选。真实联调中稳定漏掉的候选与同批次已返回行按共享归一化算法是同一路径，而不是独立功能；因此 Java 在调用模型前按候选功能名称的 NFKC、strip、连续空白折叠和小写结果预分组，只将每组最早成员作为模型代表。只有代表返回同一归一化路径的单一路径非“拆分”结论时，Java 才可将同一类型、逐字业务路径和合法 anchor 确定性投影到同组每个原始候选，并为每个成员重建自己的 `documentId`、`unitId`、`candidateIds` 和稳定顺序。每个投影成员的可读证据只从该成员已持久化的候选证据提取，绝不复制代表模型行或代表成员的专属证据文字。已接受的同路径单一路径结论可直接投影到后续批次成员而不重新请求模型。不同路径、路径漂移、代表“拆分”结论、代表坐标/候选绑定错误或缺失代表一律失败关闭。仅当同一批次已经连续三次被拒绝，且三次的每一个封闭类别都属于批准集合 `MISSING_TARGET`、`REPRESENTATIVE_BINDING`、`BUSINESS_PATH_STRUCTURE`（允许混合）时，才丢弃该批次三份未接收结果，并将该批次每个模型代表目标按原稳定顺序逐一作为单目标重新调用隔离 Skill；每项仍使用完整候选上下文、精确机器绑定、严格 Markdown、路径、anchor 和跨批次校验，最多三次。任一尝试为 `STRICT_MARKDOWN`、`ANCHOR_CONFLICT`、`NORMALIZED_PATH_CONFLICT`、`UNKNOWN_CONTRACT` 或任何其他非批准类别时，绝不逐项复核并直接失败关闭。全部单目标结果通过后，才按该批次原始稳定顺序原子合并；任一单目标失败均不持久化部分结果。该补偿不自动纠正模型分类、路径或来源坐标，不降低冻结或 `2N` 门禁，也不跨任务复用。

每页最多三次严格重试。每行的 `candidateIds` 只绑定本页自身目标，另携带内部 `groupAnchorId`：同一语义组必须选择全量稳定候选顺序中最早的 candidateId，anchor 自身必须自指，同一 anchor 的分类和对象/功能点路径必须逐字一致。本页代表目标区在每个目标的同一行列出精确 `candidateId`、`documentId`、`unitId` 与可读功能文本；模型必须逐字复制该同一行的三项机器绑定，禁止从全量上下文中同名或同路径邻居复制来源坐标。后续页还必须收到此前已接受 self-anchor 的业务路径、分类与 anchor；若本页出现相同归一化业务路径，必须逐字复制此前的业务路径和分类并沿用 anchor，不能因模型分页而重新判定或改写路径。若模型仍返回可解析但与已接受结论不一致的当前目标，Java 保持拒绝，并在下次尝试为每个该目标写入由已接受结论确定性派生的 `targetCandidateId`、分类、逐字业务路径和 anchor；代表证据绑定错误则只追加固定、非敏感指令，重申必须复制同一目标绑定行、不得复制同名/同路径邻居坐标。这不是回灌原始异常或未接受模型文本，且不改变 Java 的精确验证。分页与冻结层使用完全相同的业务路径身份：NFKC、首尾 strip、连续空白折叠为一个空格并转为小写；同一归一化路径必须使用同一最早 anchor 和同一分类，且单个“拆分”结论内的归一化路径必须互异。Java 在页面通过既有严格 Markdown parser 后立即复用冻结层完整路径结构校验：非拆分不得含 literal `<br>`；拆分必须由 literal `<br>` 分隔至少两项，每项非空纯文本且归一化互异，再验证这些锚点和路径约束；对象/功能点列只承载业务功能名或业务路径，章节号、条款号和目录编号只承载在证据对照列。真实 V2.0 第 22/30 页重复出现非拆分路径复制证据 `<br>` 后章节号的固定错误时，重试只追加固定指令，要求删除该编号，不得裁剪 Java 已接收或模型返回内容。未知/后序/非自指 anchor、类型/路径漂移、拆分内归一化重复、重复/遗漏目标、坐标缺失及任何格式错误均失败关闭，并只向下一次尝试追加固定、非敏感的格式反馈，绝不回显原始异常。业务路径保持纯文本：`<` 及其可能形成的标记继续拒绝；单独的 `>` 仅作为可读层级分隔符（例如上级到下级）被允许。该例外不影响 Markdown、候选精确绑定、anchor、归一化去重、冲突或机器 token 门禁。

所有页面成功后，Java 以全量稳定候选顺序归并每个 anchor 组，稳定生成结论序号、candidateIds 和坐标证据，并再次验证全量 union 恰好一次和每个来源坐标。只有该最终集合成功后才调用一次持久化；任何页面或归并失败均不留下部分结论，安全重试从整次核对开始。该设计不是彼此独立的分块判断：每个目标始终可比较全部候选，跨页关系通过可验证的最早锚点闭合，因而不会改变遗漏、冲突、重复、拆分或合并的全局语义及冻结来源集合。

同一批次出现归一化路径冲突时，bulk 第 2/3 次按每个冲突组列出该组全部 `targetCandidateId`、按全量稳定候选顺序确定的 `earliestTargetCandidateId`、同一实际值的 `requiredGroupAnchorId` 和固定规则，绝不回灌未接收回包的路径、分类或正文。模型若仍判断为同一路径，每行 `groupAnchorId` 必须逐字复制该组 `requiredGroupAnchorId` 的实际值，且分类和完整路径一致；若不同，必须以正式证据给出真实可区分路径及各自合法 anchor。各组保持清晰分隔，Java 只验证而不作语义裁决或改写。`NORMALIZED_PATH_CONFLICT` 不属于逐项复核批准集合，三次耗尽保持失败关闭和零单目标调用；此前已接受跨批次先例仍使用既有强制精确绑定。

若 `validatePageConclusions` 已在最终 projected 当前行发现其 `groupAnchorId` 指向 `acceptedByCandidate` 的前批 anchor、但当前类型或完整路径不等于该 accepted 结论，则 bulk 第 2/3 次只为每个此类当前行追加 `targetCandidateId`、`rejectedGroupAnchorId` 和同一实际当前值的 `requiredSelfAnchorId`。规则仅要求两条可验证分支：不逐字复用已接受先例区分类和完整路径时逐字使用该实际 self-anchor；确属旧语义组时逐字复用分类、完整路径和旧 anchor。提示不复制未接收回包的业务文本、分类、路径或正文；Java 保持仅验证，不改变解析、校验、结果或锚点。`ANCHOR_CONFLICT` 继续不是批准逐项复核类别，三次耗尽为零单目标调用、零持久化的失败关闭。

最终核对页三次耗尽时，领域层仍构造类型化异常：任务快照仅保留 1-based 页序号、总页数、固定尝试次数和封闭失败类别；若进入单目标补偿后耗尽，还可额外保留该页内的 1-based 目标序号/总数，绝不保留目标 ID。类别只可由既有固定校验消息映射为 Markdown、目标覆盖、代表证据绑定、业务路径结构、锚点、归一化路径冲突或未知合同。应用新增基于既有 Logback 的滚动文件 appender，按任务、阶段、页和尝试关联记录请求提示词、SSE 终态 Markdown 与校验失败上下文；它不将这些诊断内容投影到任务详情、列表或工作簿。无论用户允许何种业务调试内容，API Key、密码和鉴权头始终在写入前脱敏。日志路径、大小和保留天数通过运行配置提供默认值，且不新增数据库迁移。

诊断日志以一个小而深的 `WorkflowDiagnostics` module 为 seam：调用方只提交 taskId、阶段、页/尝试和文本 payload；module 统一产生关联头、删除凭据形态并交给名为 `workflow.diagnostics` 的 Logger。`FeatureAuditService` 仅记录最终核对每次请求、SSE 终态 Markdown 和拒绝原因；`GenerationWorkflow` 仅记录每批生成请求、SSE 终态 Markdown 和批次接收/失败。Logback 只为该 Logger 配置 `${TESTCASE_DIAGNOSTIC_LOG_FILE:-./var/logs/testcase-agent-diagnostics.log}` 的日/大小滚动文件，默认单文件 100 MiB、最多 30 天、总计 2 GiB。任务详情、数据库快照、Excel 与普通 REST 日志不依赖或读取该文件；运行账户对日志目录的访问控制由部署负责。该 module 不保存 final-reconciliation 页的持久化 checkpoint。

### 4. 仅在审查台账完整后冻结并集

最终集合是经可追溯的拆分、合并和去重决策后的功能清单与需求功能的并集。稳定功能标识源自冻结的文档/Chunk 证据引用集合和归一化业务路径，而非模型序号。重试复用冻结功能 ID、顺序、来源和审查结论。

在所有清单单元和候选项均处于终态且稳定功能集合已耐久化前，不创建任何生成批次。后续批次响应不得引入新功能或改变冻结映射。

### 5. 按阶段准备测试专业 Skill 会话，并保持模型输出为严格 Markdown

双向审查阶段在隔离 Skill 入口请求中仅传入 `skill_names=["feature-scope-reconciliation"]`；冻结单功能生成阶段仅传入 `skill_names=["functional-testcase-design"]`。每个材料单元、最终双向核对和单功能生成批次先以其冻结范围创建独立准备会话；准备 turn 的 `query` 固定为非检索 token `你好`，避免 KEE 将准备说明预执行为 RAG，而精确 `skill_names` 是服务端可用范围约束。准备和业务请求一律使用 `POST /agent-chat/{session_id}/isolated-skill`，旧入口的 404 不得回退。Java 在每次隔离 SSE 的同一 `tool_call_id` 上观察精确 `read_skill` 的声明、参数补齐、成功 `tool_result` 配对及显式完成终态；除 `read_skill` 和可选 `execute_skill_script` 外的任意工具均失败关闭。不得跨材料单元、最终核对或功能批次复用会话，以避免历史上下文串扰和无界增长；每个业务流仍须有显式完成终态。这样不把 92 个材料单元都交给模型临场决定是否读取同一规则，也不把固定 token、提示词或普通回答误作 Skill 证据。

准备失败时，Java 只对连接、超时或 408/429/502/503/504 等明确暂态传输错误，最多以三个新的隔离会话进行有界重试；范围、HTTP 永久错误、SSE/Markdown 合同、准备流任意 `error`、错误或缺失的精确 Skill 证据及准备阶段任意非 `read_skill` 工具调用均立即失败关闭。每次仍必须重新观察真实 SSE 工具成功事件，绝不将前一会话的证据移植到下一会话。任一材料审查单元或最终核对的准备失败会记录该项失败并终止当前审查阶段，由上层将任务置为 `FAILED`，不得继续领取后续审查单元；生成准备耗尽会终止当前任务而不继续后续批次。会话、Skill、智能体、材料类型或冻结范围任一不匹配均失败关闭。Java 不配置 KEE sandbox mode，也不依赖 Skill 的版本、哈希、manifest、outcome 或新 list/read API DTO。前者只负责清单/需求双向核对和最终生成范围建议，后者只对 Java 已冻结的功能设计用例。

内部扫描/审查调用使用由 Java 提供源单元标识符的、刻意保持较小的固定 Markdown 表。最终生成响应必须且只能包含两个 H2：`## 需求与功能清单审查发现`、`## 测试用例`；紧随其后的表头必须分别精确为 `序号｜对象/功能点｜问题分类｜证据对照` 与 `用例名称｜功能模块｜前提约束｜执行步骤｜预期结果｜对应需求内容`。表内多项仅用 `<br>` 分隔。Java 拒绝 JSON、代码围栏、标题/列改名、缺表、多表、少表、额外业务表或额外列。每次生成调用恰好接收一个冻结功能及其已授权源上下文。

Java 仅在下列条件均满足时接收一个批次：

- 响应到达显式 SSE 完成终态事件；
- 固定 Markdown 表解析成功；
- 测试表恰好包含当前冻结功能的两行；
- 一行名为 `<功能名>_正向`，另一行名为 `<功能名>_反向`；
- 步骤和预期结果非空，并保留成对编号；
- 需求文本和审查证据保持在已确认的正式文档/Chunk 引用范围内；
- 通用例外内容精确标记为 `依据通用经验，待确认`，而不得呈现为正式需求事实；Good/Bad 示例只能影响方法与结构，绝不作为正式业务证据。

第一个 Excel Sheet 从耐久主审查台账生成，而不是盲目追加重复的单批次 `未发现问题` 行；“证据对照/对应需求内容”沿用 KEE 现有问答来源的文档/Chunk 引用。第二个 Sheet 从已接收的测试用例行生成。工作簿严格保持两个 Sheet，不建设第二套来源证据。内部单元审计所需 `documentId`、`unitId`、`candidateIds`、`groupAnchorId` 在任务详情、最终 Markdown 和工作簿投影前确定性移除；导出器仍看到该 token 时拒绝导出，而不能仅隐藏页面字段。

### 6. 将完成设为跨阶段不变量

`AUDITING` 具有耐久的业务子阶段：材料清单、功能清单扫描、需求扫描第一遍、需求扫描第二遍、双向核对和功能冻结。

`COMPLETED` 要求以下条件全部满足：

- 每份当前最新、已授权源文档均在每页严格范围和文档白名单校验下读取，且仅在唯一且有序的 Chunk 已收齐、累计数量等于 `total_units`、末页 `complete=true` 且无游标循环/缺口后通过已遍历解析材料门禁；
- 每个源单元均恰好一次被接收，且两遍需求扫描均处于终态；
- 每个源候选项均有一个终态审查结论；
- 不存在未解决的冲突或映射；
- 大小为 `N` 的最终功能集合已冻结；
- 每个可生成的功能均有一个已接收批次；
- 每个已接收功能均贡献恰好两行测试用例；且
- 工作簿回读校验成功。

仅在材料台账和冻结集合完整后，当已记录为证据不足的功能无法生成或生成批次永久失败时，才允许进入 `PARTIAL`。若暴露其制品，制品必须明确说明覆盖不完整，且仅包含完整审查 Sheet 和已接收的测试用例行。清单缺口、游标错误、计数不一致、超大单元、截断、扫描未收敛或未解决映射均为 `FAILED`，且不生成制品。`CANCELLED` 保留耐久工作，但绝不将其呈现为完整。已接收批次及其两条用例行在同一数据库事务中耐久化；任务在生成中失败、进程重启或用户重试时，只重新领取未接收的批次，已接收批次不可再次调用或覆盖。未创建生成批次的审查失败会在任务结果快照中保存一条经脱敏的业务原因，并在详情/列表的已有 failure summary 投影中使用它；开始新的无批次审查重试时，系统与 `FAILED` 到 `QUEUED` 的转换在同一事务中清除该失败快照，防止旧原因在重试中或后续成功状态复现。原始模型输出、提示词、候选坐标和异常堆栈不持久化到浏览器投影，但可写入受控的持久诊断日志。

ALL 冻结功能的失败批次进入第 2/3 次尝试时，仓储层只读取同一批次、紧邻当前尝试的上一条 `FAILED` attempt 的既有 `failure_reason`，不新增表或列。失败批次筛选、仍为 `FAILED` 的条件更新、新 attempt 写入、终态任务的制品元数据清理及到 `QUEUED` 的转换处于同一事务；任一新 attempt 写入失败时全部回滚，重复或并发重试不得为同一批次增加额外尝试。工作流绝不直接拼接该值，只对白名单内的七类既有固定校验消息映射固定中文纠错规则：通用经验固定文案、步骤/预期编号一一对应、审查证据保留当前冻结 `candidateIds`、最终表后无内容、单元格仅文本且只以 `<br>` 换行、需求内容保留当前冻结 `candidateIds`、需求内容不得引用其他目标候选。未知或可疑原因只产生不含原文的固定通用合同提醒。首轮 ALL 提示不追加纠错；既有 `FEATURE` 指定功能模式不读取失败原因且提示逐字保持不变。该机制只帮助模型纠正下一次输出，不自动修改结果，不放宽 parser、冻结证据、原子接收、`2N` 或最终发布门禁。

真实第 2 次生成重试表明，通用经验固定文案仍可能被模型与正式证据写法混合。该白名单分支因此进一步固定为二选一合同：有正式材料时使用可读摘要加 `<br>candidateIds`；仅依据通用经验时，单元格完整内容只能是 `依据通用经验，待确认`，并在最后一个字后结束，不得追加 `<br>`、`candidateIds`、标签、引号、句号或任何其他文字。该收紧只改变第 3 次重试请求中的固定说明，不读取或回灌失败单元格，不自动裁剪既有响应。

### 7. 保持常用 Web 路径面向业务

创建表单保持不变。任务详情展示阶段及材料单元已处理数、功能清单候选项、功能清单遗漏、需求未覆盖的功能点、冲突、合并/拆分计数、冻结功能和已生成测试用例进度等计数。它不暴露 KEE ID、单元 ID、游标、提示词、原始 Markdown 或模型协议字段。材料读取失败消息只使用遍历、游标、计数或超大单元等业务可解释原因，不得称为版本漂移。

## Risks / Trade-offs（风险/权衡）

- **[当前 KEE 镜像已回滚]** → Java 先以 WireMock 和确定性 fixture 验证已冻结的消费者合同；真实联合验收仅在临时部署两个已验证 KEE change 后执行，期间不得弱化 Java 门禁。
- **[两遍需求扫描增加成本和时长]** → 使用有界耐久工作项、五个任务槽位、可恢复尝试和每调用预算；第 1 阶段没有产品时长限制。
- **[模型可能对语义拆分/合并决策产生分歧]** → 要求源完整覆盖，保留全部来源追溯信息，在有界范围内重试未收敛情形，并应失败而非静默丢弃候选项。
- **[功能清单重复展示序号或格式错误会放大候选项]** → 保留每次候选出现，报告异常，仅通过显式结论合并。V2.0 序号 113 重复是必需的回归用例。
- **[单个 Chunk 超过页上限]** → 返回 `unit_too_large` 并失败关闭；不得以裁剪文本换取完成状态。
- **[部分制品可能被误解]** → 将任务和工作簿标记为不完整，暴露原因，且绝不使用通用的已完成措辞。
- **[新增审查表增加持久化量]** → 分页读取和 repository 查询；仅保存可复现所必需的有界源文本和文档/Chunk 引用，不保存原始二进制文件。
- **[诊断日志包含模型原文与内部坐标]** → 仅落入受控本地滚动文件，不进入共享 API/工作簿；即使如此，凭据类形态仍在写入前统一脱敏，且以 30 天/2 GiB 默认上限限制磁盘占用。

## Migration Plan（迁移计划）

1. 固化已完成 KEE change 的消费者 HTTP/SSE 合同；当前运行镜像保持回滚状态，Java 不修改 KEE。
2. 为清单、候选项、结论、冻结和可恢复审查工作添加 Java RED 测试及 Flyway 表。
3. 编写 WireMock 消费方 fixture，覆盖固定 URI/DTO、每页严格范围和文档白名单、默认/最大/超限收敛分页、4 MiB 页上限、所有显式业务错误、`unit_too_large` 不截断、唯一有序 Chunk、`ordinal`/`total_units` 计数、仅末页 `next_cursor=null`/`complete=true`、游标循环/缺口，以及调用方明确报告替换后的首页重启；并为两个精确 Skill 的 `read_skill` 成功/缺失/失败 SSE 事件编写 fixture。
4. 实现有界扫描、两遍需求收敛、双向核对和冻结功能规划。
5. 在同一 Web 操作后替换当前一次性 `ALL` 发现路径；保持指定功能生成兼容。
6. 增加批次、终态、两个 Sheet 导出和 Web 进度门禁。
7. 运行确定性合成验收；随后在临时部署两个 KEE change 时执行一次有界的 V2.0 联合验收。当前九功能/十八用例结果是负向回归 fixture，不会被改写。

回滚将禁用新的 `ALL` 路径，并保留其应用自有审查数据用于诊断。它不回退或变更 KEE 文档。旧的一次性路径不得作为静默回退重新启用。

## Open Questions（待确认问题）

- 联合验收前需要确认临时 KEE 部署确实包含提交 `5f1d1290`、`e241c53a`、`83227ac4`、`9e627423` 对应的实现/验证证据，且 sandbox mode 已由 KEE 部署设置为非 disabled；Java 不配置该运行条件。
- 对于审查覆盖完整但测试用例生成不完整的有效 `PARTIAL` 制品，其产品措辞需要最终 UI 文案评审。
