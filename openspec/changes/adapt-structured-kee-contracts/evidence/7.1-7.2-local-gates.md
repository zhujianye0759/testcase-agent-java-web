# 7.1 / 7.2 本地收口证据

日期：2026-08-21

分支：`codex/adapt-structured-kee-contracts`

验证起点：`49f7684bfab111962b6a3399c2c585b206e23fe8`

## 旧路线隔离审计

- `GenerationWorkflow.executeClaimed` 对无指定功能的 `ALL` 任务先调用生产必需的
  `StructuredAllGenerationCoordinator`，随后立即返回。生产配置强制注入真实
  `DefaultStructuredAllGenerationCoordinator`，不存在空协调器回退旧路线的生产装配。
- 路由隔离测试证明新 `ALL` 不调用旧材料遍历入口、旧功能审查/冻结、
  `prepareGenerationSession`、普通 Agent `invoke`、旧批次入口或 `exportMarkdown`。
  因而旧分支内保留的 SSE、Markdown parser、固定重试提示和固定两条/`2N`
  合同不会进入新 structured ALL 生产调用链。
- `DefaultStructuredAllGenerationCoordinator` 只通过 structured Skill 端口执行同步 JSON
  调用，按需求事实建立非固定数量测试点，并从已验收持久化投影调用
  `exportStructured`；结构错误不会回退 Markdown。
- `git diff --name-status cfd179d...49f7684 -- openspec/changes/all-completeness-cross-audit`
  无输出。旧 change 未修改、未删除、未归档。

## 保留的共享能力

- parsed-units：继续使用 `ParsedUnitCatalogPort`、完整分页校验和
  `RequirementMaterialTraversalService`，但由新协调器消费已冻结的完整材料。
- 任务耐久性：继续使用任务仓储、五槽队列、启动恢复和 V10 structured 工作项/尝试表。
- 取消与重试：继续使用持久化取消检查；structured 工作只允许
  `model_unavailable|model_execution_failed` 对同一 work 再尝试一次。
- 双 Sheet：继续由 Apache POI 生成且回读验证恰好
  “需求与功能清单审查发现”“测试用例”两个 Sheet；structured 页面和 Excel
  共用已校验持久化投影。

## 本地验证结果

- 后端全量：326 项，323 通过，3 项失败。相对已在同一 JDK/环境对
  `cfd179d` 完成的基线复跑（253 项、250 通过、同 3 项失败），新增的 73 项均通过；
  失败方法和失败语义未变化：
  - `AllCompletenessGoldenFixtureTest#completesTheMappedGoldenFixtureWithStableNAndExactlyTwoNRows`：
    `FinalReconciliationPageException`，第 1/4 页固定合同连续三次未满足；
  - `AllCompletenessGoldenFixtureTest#blocksFreezeWhenTheFullyDisposedGoldenLedgerStillContainsAnUnresolvedConflict`：
    同一 `FinalReconciliationPageException`；
  - `MarkdownBatchPersistenceIntegrationTest#projectsOnlyTheSafeFinalReconciliationPageSummaryToTaskDetailAndList`：
    旧 JSON 字符串期望冒号后无空格，实际序列化结果有一个空格。
  未修改旧 fixture 或固定 `2N` 测试放行。
- structured 后端聚焦：88/88 通过。
- MySQL/Testcontainers 聚焦：35/35 通过，其中 Store 23、任务恢复 4、迁移/生产装配 4、
  结构化详情投影 4。
- 普通 Agent 兼容回归：`WebClientKnowledgeAgentAdapterTest` 35/35 通过。
- 前端：Vitest 38/38 通过；typecheck、lint、production build 均通过。
- `openspec validate adapt-structured-kee-contracts --strict`：通过。
- `git diff --check`：通过。

## 未执行的外部门禁

KEE `4c68f2f8` 尚未收到集成部署和精确镜像通知，因此未执行真实 parsed-units、
三个 Skill/双 operation、十类错误、repair 标志和普通 Agent 非干扰联合 E2E。
任务 7.3 保持未完成。
