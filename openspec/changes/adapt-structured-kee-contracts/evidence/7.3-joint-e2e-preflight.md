# 7.3 本机联合验收预检证据

日期：2026-08-21

## KEE 运行身份

- 源提交：`775ad6c6f4591cf45e2a8b23be0b81ce8107ec64`。
- 镜像 ID：`sha256:d3ce51bd95667449254bb7201ca7724ee57ffe1caadabd44793ed029b02088f4`。
- 容器 ID：`1ac0eca2723e5d4aad8ee3220572f0ef93f39e8737d9460ebd5ba65a455b56f8`。
- 容器启动时间：`2026-08-21T00:31:23.462417464Z`。
- 容器健康状态为 `healthy`，`/health` 返回 HTTP 200；`/api/v1/system/info`
  的 `commit_id` 与源提交精确一致。

## 只读能力与材料检查

- 既有 API Key 可读取知识库目录、目标 Agent、启用模型和 parsed-units；检查过程未输出凭据。
- 目标 Agent 使用活动模型 `qwen3.7-plus`，Skill 选择模式为 `all`；容器内存在冻结的三个
  Skill：需求材料质量审查、功能范围核对、功能测试用例设计。
- “战略运管系统”四份材料均为 `completed/enabled`，文件哈希存在，材料类型为
  `function_list` 与 `work_order_plan`；V1.0/V2.0 各有一份功能清单和一份工单方案。
- 四份材料的 parsed-units 均完成稳定全遍历：V2.0 为 60 + 16 个单元，V1.0 为
  4 + 9 个单元；无重复、无遗漏、ordinal 连续，最终页均为 `complete=true` 且
  `next_cursor=null`。
- 阻断：四份材料返回的 `knowledge_scope` 都不含 `project_id` 字段。冻结合同要求
  `project_id` 非空，Java 动态目录也必须拒绝空项目，所以当前合格白名单数量为 0。
  在材料作用域修正前，不得伪造项目字段、绕过 task-options 或创建真实任务。

## Java 数据库与重复任务检查

- 共享应用库已合法应用旧 V10 `add generation recovery runs` 与 V11
  `add isolated skill execution evidence`；未人工修改 `flyway_schema_history`。
- 当前 change 原结构化 V10 尚未发布，因此已按首个空闲版本顺延为
  `V12__persist_structured_generation_results.sql`；其他工作树未发现 V12 占用。
- 共享应用库中运行中的 ALL 任务为 0，占用执行槽为 0；未创建真实任务。
- V12 RED→GREEN：改名前，真实 MySQL 测试查不到 V12；改名并清理旧构建产物后，
  Flyway 历史精确记录 version 12 和目标脚本。
- MySQL/Testcontainers 聚焦 32/32、structured 聚焦 108/108 通过；全量后端
  327 项中 324 通过，仍只有已证明的 3 个旧基线失败。

## 7.3 状态

真实任务、三 Skill/双 operation、同任务页面/Excel、十类错误与普通 Agent 非干扰的
运行时验收尚未执行。`project_id` 材料门禁修正并重新确认唯一白名单前，7.3 保持未完成。

## Controlled R5 安全尝试与回滚

- 受保护的 R4 夹具工作树保持未跟踪原状；没有修改、移动或重写三份源文件。
- 正常 API 的无效请求探针确认版本管理与文档上传权限可用（均为 HTTP 400，而非
  403），显式 `project_id=controlled-ca-login-r5` 的 scope preview 返回 HTTP 200。
- 创建了唯一临时版本 key `ca-login-20260821-r5-structured`，上传 scope 只增加专用
  非敏感 project_id，其余类型坐标仍为 system_version、admission_material 和三种
  冻结材料类型。
- 第一份同源夹具上传被 KEE 以 HTTP 409 拒绝。代码级根因是同一知识库按文件名、
  大小和文件哈希做重复检测；重复文档已经属于 R4 的无 project_id 作用域，服务按
  “重复文档已属于不同的知识范围”失败关闭，不能把同一文档静默换绑到 R5。
- 自动回滚已完成：R5 未产生可见文档，随后通过正式版本删除 API 删除 R5。复核结果为
  R4 版本 1 个且仍有 3 个 completed/enabled 文档，R5 版本 0 个；Java task-options
  仍为 0 个候选，独立验收库 generation_task 为 0。
- 回滚后的 R4 parsed-units 再验证为 3 个文档、合计 3 个单元，最终 cursor 均为 null
  且均有显式 complete；R4 的既有解析证据未受影响。
- 结论：在“不改 R4 scope、不改用户夹具、不人工改数据库”的边界内，没有安全路径
  形成带非空 project_id 的三材料白名单。未创建真实任务，也没有 taskId、页面或制品。
