# 7.3 本机联合验收预检证据

日期：2026-08-21

## KEE 运行身份

- 源提交：`775ad6c6f4591cf45e2a8b23be0b81ce8107ec64`。
- 镜像 ID：`sha256:d3ce51bd95667449254bb7201ca7724ee57ffe1caadabd44793ed029b02088f4`。
- 容器 ID：`1ac0eca2723e5d4aad8ee3220572f0ef93f39e8737d9460ebd5ba65a455b56f8`。
- 容器启动时间：`2026-08-21T00:31:23.462417464Z`。
- 容器健康状态为 `healthy`，`/health` 返回 HTTP 200；`/api/v1/system/info`
  的 `commit_id` 与源提交精确一致。

## 只读能力与材料检查

- 既有 API Key 可读取知识库目录、目标 Agent、启用模型和 parsed-units；检查过程未输出凭据。
- 目标 Agent 使用活动模型 `qwen3.7-plus`，Skill 选择模式为 `all`；容器内存在冻结的三个
  Skill：需求材料质量审查、功能范围核对、功能测试用例设计。
- “战略运管系统”四份材料均为 `completed/enabled`，文件哈希存在，材料类型为
  `function_list` 与 `work_order_plan`；V1.0/V2.0 各有一份功能清单和一份工单方案。
- 四份材料的 parsed-units 均完成稳定全遍历：V2.0 为 60 + 16 个单元，V1.0 为
  4 + 9 个单元；无重复、无遗漏、ordinal 连续，最终页均为 `complete=true` 且
  `next_cursor=null`。
- 阻断：四份材料返回的 `knowledge_scope` 都不含 `project_id` 字段。冻结合同要求
  `project_id` 非空，Java 动态目录也必须拒绝空项目，所以当前合格白名单数量为 0。
  在材料作用域修正前，不得伪造项目字段、绕过 task-options 或创建真实任务。

## Java 数据库与重复任务检查

- 共享应用库已合法应用旧 V10 `add generation recovery runs` 与 V11
  `add isolated skill execution evidence`；未人工修改 `flyway_schema_history`。
- 当前 change 原结构化 V10 尚未发布，因此已按首个空闲版本顺延为
  `V12__persist_structured_generation_results.sql`；其他工作树未发现 V12 占用。
- 共享应用库中运行中的 ALL 任务为 0，占用执行槽为 0；未创建真实任务。
- V12 RED→GREEN：改名前，真实 MySQL 测试查不到 V12；改名并清理旧构建产物后，
  Flyway 历史精确记录 version 12 和目标脚本。
- MySQL/Testcontainers 聚焦 32/32、structured 聚焦 108/108 通过；全量后端
  327 项中 324 通过，仍只有已证明的 3 个旧基线失败。

## 预检时的 7.3 状态

真实任务、三 Skill/双 operation、同任务页面/Excel、十类错误与普通 Agent 非干扰的
运行时验收尚未执行。`project_id` 材料门禁修正并重新确认唯一白名单前，7.3 保持未完成。

## Controlled R5 安全尝试与回滚

- 受保护的 R4 夹具工作树保持未跟踪原状；没有修改、移动或重写三份源文件。
- 正常 API 的无效请求探针确认版本管理与文档上传权限可用（均为 HTTP 400，而非
  403），显式 `project_id=controlled-ca-login-r5` 的 scope preview 返回 HTTP 200。
- 创建了唯一临时版本 key `ca-login-20260821-r5-structured`，上传 scope 只增加专用
  非敏感 project_id，其余类型坐标仍为 system_version、admission_material 和三种
  冻结材料类型。
- 第一份同源夹具上传被 KEE 以 HTTP 409 拒绝。代码级根因是同一知识库按文件名、
  大小和文件哈希做重复检测；重复文档已经属于 R4 的无 project_id 作用域，服务按
  “重复文档已属于不同的知识范围”失败关闭，不能把同一文档静默换绑到 R5。
- 自动回滚已完成：R5 未产生可见文档，随后通过正式版本删除 API 删除 R5。复核结果为
  R4 版本 1 个且仍有 3 个 completed/enabled 文档，R5 版本 0 个；Java task-options
  仍为 0 个候选，独立验收库 generation_task 为 0。
- 回滚后的 R4 parsed-units 再验证为 3 个文档、合计 3 个单元，最终 cursor 均为 null
  且均有显式 complete；R4 的既有解析证据未受影响。
- 结论：在“不改 R4 scope、不改用户夹具、不人工改数据库”的边界内，没有安全路径
  形成带非空 project_id 的三材料白名单。未创建真实任务，也没有 taskId、页面或制品。

## Controlled R5 正式材料

- 后续使用独立临时文件形成唯一 R5，未修改、移动或覆盖 R4。R5 版本 ID 为
  `ea947a7b-f37a-4e59-95b8-3d7c1d65dbb4`，专用项目标识为
  `controlled-ca-login-r5-20260821-0934`。
- 三份文档 ID 为 `84195699-f430-4efc-9660-294b5ae8be52`（功能清单）、
  `5ebb9844-a013-45e9-9b98-db4e1482282e`（需求规格说明）和
  `9f086de0-9c97-425f-9c8f-db8ca02b3273`（工单方案）。创建任务前，Java
  `task-options` 只返回该 R5 一个合格版本，包含 3 类材料和 3 份文档。
- 三份文档分别完成 1 页、1 单元的 parsed-units 遍历；每份均满足单元 ID 唯一、
  `ordinal=1`、声明总数与收集数一致、最终 `complete=true` 且
  `next_cursor=null`。该证据只证明持久化解析文本可完整、确定遍历，不证明原始
  Word/PDF/Excel 版面坐标完整。

## 唯一真实任务与结构化执行

- 唯一真实 ALL 任务为 `e36b0a3a-3714-45bb-b502-0bec967942f6`；未创建第二个
  业务任务。任务终态为 `COMPLETED`，结构化处理状态为 `COMPLETED`，覆盖结果为
  `COMPLETE`。
- 持久化工作项共 15 个且全部完成：需求材料审查 2 个、
  `extract_function_list` 1 个、`reconcile` 1 个、功能测试用例设计 11 个。
  这证明三个 Skill 和 `feature-scope-reconciliation` 的两个 operation 均走同步
  结构化入口，没有回退 SSE/Markdown。
- 首次执行中，同一用例设计工作项收到一次 KEE `model_execution_failed`。仅调用一次
  既有 `POST /api/tasks/{taskId}/retry` 后，attempt #1 保持
  `FAILED/model_execution_failed`，同一个 workId 的 attempt #2 完成；其余 14 个
  attempt #1 完成。已完成的上游工作没有重复执行。
- 原子验收后的业务行计数为：需求事实 2、审查发现 3、功能清单项 1、核对记录 1、
  测试点 11、测试用例 20、步骤 63。制品重生成前后这些计数完全一致。

## 同步外壳、错误和普通 Agent 非干扰

- 真实任务的成功调用均返回 HTTP 200 同步 JSON 外壳，包含固定
  `schema_version=1.0`、精确 `skill_name` 和布尔 `repair_attempted=false`；失败调用
  返回 HTTP 500 且仅使用稳定类型 `model_execution_failed`，没有业务结果。
- 对部署实例执行了不触发模型的安全错误探针：未知字段得到
  `400/invalid_request`，不存在会话得到 `404/session_not_found`，无权 Agent 得到
  `403/forbidden`，未支持 Skill 得到 `400/unsupported_skill`。没有为制造错误而停用
  模型、删除 Skill 或扩大模型响应。
- Java 消费者合同测试 `StructuredSkillWireMockTest` 19/19 通过，逐一验证全部十类
  稳定错误：`invalid_request`、`request_too_large`、`session_not_found`、`forbidden`、
  `unsupported_skill`、`skill_unavailable`、`model_unavailable`、
  `model_execution_failed`、`structured_output_invalid`、`response_too_large`；其中
  `structured_output_invalid` 的测试外壳明确携带并保留一次
  `repair_attempted=true`。超大请求在 Java 发网前拒绝，超大响应和非 2xx 伪成功均
  失败关闭。
- 另建普通聊天会话调用非 isolated 的 `POST /agent-chat/{sessionId}`，返回 HTTP 200、
  `text/event-stream`，并包含显式 `complete/done=true` 终态。结构化接口改造未替换或
  破坏普通 Agent SSE 路径。

## 同任务安全重生成、页面与 Excel

- 在重启 8082 前已依次通过 `openspec validate adapt-structured-kee-contracts --strict`、
  `git diff --check`、敏感信息形态检查和精确差异范围复审。8082 随后从当前 Java
  工作树启动，Flyway 校验 10 个迁移并确认验收库版本为 V12。
- 只调用一次 `POST /api/tasks/e36b0a3a-3714-45bb-b502-0bec967942f6/artifact/regenerate`，
  返回 HTTP 204。条件更新把原制品 `5737e442-1cff-41e0-a350-4c8f5ab76a8c`
  替换为 `6e822549-0248-4d5c-a1f4-c0516b1d4174`，没有修改结构化业务表。
- 新制品路径为
  `D:\workspace\testcase-agent-java-web\.worktrees\adapt-structured-kee-contracts\var\e2e-artifacts\6e822549-0248-4d5c-a1f4-c0516b1d4174.xlsx`，
  SHA-256 为 `f9064c914ea97afd5c8ac728526b2822f04335d32c8f35ca0327a049fa6f7083`；
  本地文件、数据库和下载接口三者哈希一致。
- 原始 XLSX 恰好包含两个 Sheet：“需求与功能清单审查发现”5 行（表头 + 4 行）和
  “测试用例”21 行（表头 + 20 行）。用户可见单元格中的内部机器键和技术占位符计数
  均为 0，中文业务枚举“禁用/锁定/未激活”保持原文；H 列 20 个数据单元格均为空，
  已确认此前数字 36 是外部渲染工具误画列宽，不是 Java 导出内容。
- 页面详情与 Excel 读取同一份已验证持久化投影。1440x820、1024x820 页面均显示
  处理状态“已完成”、覆盖结果“正式覆盖完整”、四阶段真实计数和新制品下载链接；展开
  三条审查建议后，机器键与技术占位符计数仍为 0。截图：
  `D:\CodexData\.codex\visualizations\2026\08\20\01a02010-f8b8-7b91-9c6f-b7447f28e9d6\structured-all-e2e-20260821\task-detail-1440x820.png`、
  `task-detail-1440x820-expanded.png`、`task-detail-1024x820.png`。

## KEE current 版本恢复

- 页面、数据库和制品证据完成后，已通过正式 API 恢复原 V2.0
  `03e76939-4a5c-49fd-b42f-fe0732ae12c2` 为 current，接口返回 HTTP 200。
- 恢复后只读复核 current 版本恰好 1 个且为 V2.0；R5 仍保留用于本次验收取证，但
  `is_current=false`，不会继续影响普通 KEE 检索/问答。

## 最终本地门禁

- `StructuredSkillWireMockTest` 19/19、普通 Agent
  `WebClientKnowledgeAgentAdapterTest` 35/35 通过。
- MySQL/Testcontainers 聚焦 38/38 通过：结构化原子接收 Store 23、任务详情与制品
  CAS 6、启动恢复队列 4、V12 迁移 5。
- 前端 38/38、typecheck、lint、build 全部通过；1440x820 与 1024x820 真实浏览器
  页面检查通过。
- 全量后端共 351 项，348 通过；仍只有已在未修改基线 `cfd179d` 证明的 3 项旧失败：
  `AllCompletenessGoldenFixtureTest` 两项均在最终核对 1/4 页连续三次不满足旧固定合同，
  `MarkdownBatchPersistenceIntegrationTest` 一项仍是旧 JSON 期望值冒号后无空格、实际
  有空格。未修改旧 fixture 或固定 2N 断言放行，也没有出现本 change 新回归。
