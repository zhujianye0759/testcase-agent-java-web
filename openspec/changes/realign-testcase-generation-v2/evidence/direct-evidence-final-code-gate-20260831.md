# Java V2 直接依据与恢复最终代码门禁（2026-08-31）

## 结论

本轮代码门禁覆盖事实原子性、直接依据保守闭合、V2 显式窄恢复、恢复数据有界读取及历史 V1 只读兼容。生产实现未包含任务、项目、知识库、材料名称或固定业务数量特判。当前只完成代码、测试、复审和不可变制品构建，未部署、未调用 KEE、未创建或重试业务任务。

## 行为门禁

- 多个动作共享一个义务词时按复合义务拒绝；字段集合、条件从句和同一可测试行为不因连接词或标点机械误拒。
- 事实直接依据允许同一连续引文内的受控语义压缩，仍拒绝新增业务含义、跨引文拼接、错误引用和未授权证据。
- 显式恢复对格式错误的历史公开结果、空 accepted hash、投影不闭合、部分业务写入和并发竞争保持失败关闭。
- 大规模恢复只在当前固定批次加载功能文字、事实、引文、反馈、测试点、三态结果、用例、步骤、绑定、解析单元正文和 V23 回放；任务级快照仅保留工作元数据、发布哈希及集合闭合结论。
- 历史 V1 仅保留详情、制品和公开写入口的只读兼容。已退役的功能清单/需求材料双向审核、功能冻结和固定 `2N` 生成 golden fixture 不再作为新 V2 回归门禁。
- 安全失败快照按 JSON 语义校验 `failureSummary`，并继续断言内部地址、技术坐标和测试秘密不进入公开快照。

## 测试与静态检查

- V2 恢复聚焦：`StructuredGenerationAcceptanceStoreIntegrationTest` 333、`GenerationTaskRepositoryRetryTest` 6、`RequirementFactV2ValidatorTest` 51，共 390/390 通过。
- V1 只读兼容与安全快照聚焦：3/3 通过；覆盖历史 V1 所有公开写入口拒绝、详情形状不变和安全失败快照。
- 完整后端：953/953 通过，失败 0、错误 0、跳过 0；不再保留历史失败例外。
- 前端：6 个测试文件、57/57 通过；`typecheck`、ESLint、生产构建均通过。
- `openspec validate realign-testcase-generation-v2 --strict`：通过。
- `git diff --check`：退出码 0，仅存在既有 LF/CRLF 提示。
- 新增生产行敏感凭据字面量扫描：0；真实项目/任务/知识库身份扫描：0。

## 构建制品

- 路径：`backend/target/testcase-agent-backend-generation-v2-direct-evidence-codegate-20260831-B84F58AD97C3.jar`
- 字节数：54,485,250
- SHA-256：`B84F58AD97C3610C92F06C4B4EEF285B50FA10CEF2885BA8B03310B76F873DD2`

## 未执行项

- 未部署 Java 8082/5173，未对 live 数据库应用新的迁移或执行零副作用门禁。
- 未调用 KEE，未创建、重试、恢复或取消业务任务。
- 两套通用材料的 `pending_only` / `unable_to_generate` 联合验收仍待后续正式放行。
- OpenSpec 任务 6.9、6.10 因上述 live 门禁未完成而保持未勾选。
