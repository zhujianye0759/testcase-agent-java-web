# Java V2 版本化已审核测试点代码门禁（2026-09-01）

## 结论

`REQ-TGV2-016` 已完成代码、真实 MySQL、前端和静态门禁。V2 创建请求可携带零到多个版本化已审核测试点；Java 按冻结顺序持久化和执行，不用固定的缺事实占位项覆盖显式测试点。正式事实派生点与显式测试点的身份保持互斥，发生碰撞时当前事实窗口在发布前失败关闭。

实现不包含任务、项目、知识库、材料名称、页数、固定功能数量或固定测试点数量特判。两套非正式夹具的 live 验收须在最终部署与零副作用门禁通过后执行，任务 `6.10` 在此之前保持未完成。

## TDD 与恢复边界

- 创建 API 和动态范围解析支持可选 `testPoints`；旧请求省略该字段时按空集合处理。
- V25 为追加式迁移，功能与显式测试点在同一任务事务中冻结；重复身份、未知功能、内容漂移、顺序漂移和并发冲突均失败关闭。
- 协调器先执行事实派生测试点，再按冻结顺序执行显式测试点；同一功能没有正式事实时，显式测试点不会被通用缺事实占位项替换。
- 当前事实窗口若会派生出与显式测试点相同的身份，在接受事务前拒绝该窗口；此前独立完成的窗口保持不变。
- 旧恢复门要求原始任务快照、已审核范围、历史工作登记和业务投影逐项闭合。历史占位工作除身份外，还比较 Skill、operation、序号、材料、文档、来源、目标证据、上下文证据、功能、测试点、父项和拆分深度。
- 证据 JSON 的持久化表示也纳入身份：目标证据数组必须存在，空上下文必须保存为 SQL `NULL`。`[]` 与 `NULL` 互换时 `canRetry=false`，恢复接口不抛内部异常且不产生写入。

## 测试与静态检查

- 占位工作完整坐标聚焦：18/18 通过。
- 真实 MySQL 主测试：488/488 通过，失败 0、错误 0、跳过 0。
- 相关迁移、队列、事务、协调器和业务校验：116/116 通过。
- 完整后端：1165/1165 通过，失败 0、错误 0、跳过 0；Maven `BUILD SUCCESS`（2026-09-01 17:48+08）。
- 前端：Vitest 6 个文件、59/59 通过；TypeScript 类型检查、ESLint 和 Vite 生产构建均通过。
- `openspec validate realign-testcase-generation-v2 --strict`：通过。
- `git diff --check`：退出码 0，仅有 Git 的 LF/CRLF 工作副本提示。
- 新增行扫描：凭据模式 0、调试输出 0、真实样本或固定任务/知识库特判 0。

## 双轴复审

- 规格轴：P0=0、P1=0、P2=0、P3=0。
- 标准/实现轴：P0=0、P1=0、P2=0、P3=0。
- 最后一项 P2 为历史占位工作把 SQL `NULL` 与 JSON 空数组归一为同一列表；已按上述 RED→GREEN 修复并完成全量回归。

## 部署与 live 验收

本节在精确提交、不可变 JAR、唯一 8082 部署、Flyway V25、数据库零副作用门禁和两套非正式夹具验收完成后补充。
