## 1. KEE 6.2 Java OpenSpec 与版本化输入

- [x] 1.1 为 V2 工作流、输入、制品和已审核功能范围补 RED 合同测试，证明旧创建路径缺少版本/范围时会误进入旧编排
- [x] 1.2 实现版本化任务输入与范围冻结，V2 缺少已审核范围时在任何 KEE 调用前失败关闭
- [x] 1.3 为 V1 历史任务补只读详情/下载兼容回归，证明不会创建 V2 work 或改写历史数据

## 2. KEE 6.3 V2 DTO 与事实接收

- [x] 2.1 为两个 V2 Skill 的精确请求/响应外壳、字段、枚举、单文档授权和无 V1 回退补 RED
- [x] 2.2 实现 V2 专用 DTO、完整请求字节预算和按 HTTP 状态/`details.type` 的错误分类，不设置固定输出 token 上限
- [x] 2.3 为原始 parsed-unit content 超过 16 KiB、完整保真、连续目标/context 闭合补 RED→GREEN
- [x] 2.4 为事实回显、引用、连续 quote、原子性、稳定键、窗口内/跨窗口合并和零事实反馈补 RED→GREEN

## 3. KEE 6.4 V2 编排、事务与恢复

- [x] 3.1 为“新任务只调用两个 V2 Skill且旧四 operation 零引用”补公开编排 RED
- [x] 3.2 实现按函数/材料窗口/测试点局部继续的 V2 协调器，移除旧材料评审/候选/对账前置门禁
- [x] 3.3 新增 V20 向前迁移，保存 V2 版本、已审核范围、事实/引文、可测性反馈、生成结果和发布账本，不改写 V14-V19
- [x] 3.4 实现窗口/测试点单事务发布、同键同哈希幂等、异哈希拒绝、并发单赢家、失败回滚与重启只恢复未完成工作
- [x] 3.5 为 `generated`/`pending_only`/`unable_to_generate` 业务矩阵和正式证据闭合补 RED→GREEN，确保正式数据零部分发布

## 4. KEE 6.5 状态、API、页面与 Excel

- [x] 4.1 实现处理状态与覆盖结果分轴计算，并用不同系统/规模夹具证明动态用例数且无样本特判
- [x] 4.2 扩展任务详情 API 的 V2 同源投影，隐藏原始 JSON、Markdown、内部枚举/键和凭据
- [x] 4.3 实现 V2 中文详情页的反馈、正式/待确认/无法生成与双轴状态，保留旧任务页面兼容
- [x] 4.4 更新 Excel 同源导出与公式注入保护，回读证明恰好两个固定 Sheet且第二张只含正式用例

## 5. KEE 6.6 综合门禁与交接

- [x] 5.1 运行聚焦后端测试、真实 MySQL/Testcontainers 迁移/事务/并发/恢复回归并记录精确计数
- [x] 5.2 运行前端 unit/type/lint/build、Excel回读、OpenSpec strict、git diff-check和敏感信息检查
- [x] 5.3 静态证明 V2 新任务调用图对旧两个 Skill/四 operation 引用为零，历史 V1 仅保留读取兼容
- [x] 5.4 按 Standards 与 Spec Compliance 两轴复审，修复全部 P0/P1/P2 并记录 P3 结论
- [x] 5.5 整理未执行 live KEE/Java 部署和真实任务验收项，停在等待 KEE 主会话复核且不部署、不调用业务接口

## 6. 联合验收缺陷收口

- [x] 6.1 固定 `FACT_ATOMICITY_INVALID` 合成现场并补 RED，证明连接字符不能替代“两个独立义务”的业务判定
- [x] 6.2 最小修复 Java V2 事实原子性校验，保留明确复合义务、引文、直接依据和窗口零接收门禁
- [x] 6.3 为 `PARTIAL/FAILED` V2 事实窗口增加显式同任务窄恢复，保留历史 attempt 并使旧 fallback 结果退出当前投影
- [x] 6.4 运行聚焦、真实 MySQL/并发/事务、前端/Excel、OpenSpec strict、diff/sensitive 与双轴复审
- [x] 6.5 补齐已验证 `unable_to_generate` 原因差异的恢复回归，构建并部署唯一 Java 运行物，完成 live 零副作用恢复资格门禁后停在等待正式 retry 放行
- [x] 6.6 固定 `FACT_DIRECT_EVIDENCE_UNSUPPORTED` 合成现场并补 RED，证明连续引文支持的受控压缩不能被逐字子串算法误拒
- [x] 6.7 最小实现单引文内保守语义闭合，保留新增业务含义、跨引文、错误引用和零部分接收门禁
- [x] 6.8 为零写入的 V2 直接依据误拒增加同任务显式窄恢复，保留历史 attempt 和已完成范围
- [x] 6.8a 退役会重新执行旧功能清单/需求材料双向审核和固定 `2N` 的 V1 golden fixture，保留现有 V1 详情、制品及公开写入口只读兼容门禁；将安全失败快照改为按 JSON 语义而非序列化空白断言
- [x] 6.8b 按持久化工作流版本分流 Excel 名称：V2 第一张固定为“需求可测性反馈”，V1 历史导出与已有制品继续保留“需求与功能清单审查发现”；补导出回读、V2 协调器、页面与历史兼容回归
- [x] 6.9 运行聚焦、真实 MySQL/并发/事务、完整后端、前端、OpenSpec strict、diff/sensitive 与双轴复审，构建并部署唯一 Java 运行物后完成 live 零副作用资格门禁
- [x] 6.9c 为 `FACT_DIRECT_EVIDENCE_UNSUPPORTED` 增加安全规则级诊断规格，冻结有限枚举、旧行兼容、内部持久化与 API/页面/Excel/普通日志不泄漏边界
- [x] 6.9d 按公共 validator 与真实 MySQL 接缝 RED→GREEN 实现确定性多引文原因集合和 V14 无迁移存储解析，保留零部分接收、并发与反臆造门禁
- [x] 6.9e 运行聚焦、相关后端、真实 MySQL/并发、OpenSpec strict、diff/sensitive 和双轴复审，提交推送、构建精确提交并部署唯一 Java，完成 live 零业务副作用门禁
- [x] 6.9f 修复安全直接依据诊断的 `REQ-TGV2-012` 源码追踪及恢复门禁缺口：覆盖公开枚举、普通日志、attempt/work/task 持久化和详情读取入口；以真实 MySQL TDD 验证 task/work/latest-attempt 完整诊断三元组的严格解析、旧固定消息兼容、三层一致、污染失败关闭、事务回滚和并发单赢家；保持 V14 无迁移、内部原因不扩大恢复白名单，并重新完成复审、提交、精确构建部署与 live 零副作用门禁；本轮验收采用用户态详情/下载 API、数据库只读对账、Excel 程序回读和前端 unit/type/lint/build 四层证据，明确页面视觉、按钮交互和浏览器兼容性未执行
- [x] 6.9g 按 `REQ-TGV2-013` 为仅有失败 artifact、全业务表零写入的 V2 技术故障任务增加通用显式恢复：用真实 MySQL TDD 覆盖固定技术失败白名单、完整零写入闭集、V24 artifact 身份唯一索引、跨任务固定锁序、完整 attempt 历史、事务回滚、并发单赢家、下一 attempt 与 V1/业务错误拒绝；完成全门禁、提交推送、不可变 JAR 部署和 live 仅 `canRetry` 变化的零副作用证明，正式业务 retry 继续等待单独放行
- [x] 6.9h 按 `REQ-TGV2-014` 修复 V2 `expected_results` 被错误要求逐项复制步骤预期的合同收窄；以通用 validator、真实 MySQL 接收事务和 API/Excel 投影 TDD 证明总体预期独立有序、步骤编号/三态/业务依据/证据/敏感边界不放松，并为仅有零写入旧误拒的设计 work 增加同任务显式窄恢复，终止被正式事实替代的零写入 fallback、保留已完成事实/反馈/发布账本且下一执行不重跑事实；完成全回归、双轴复审、提交推送、精确构建部署与 live 零副作用资格门禁后停在 retry 前
- [x] 6.9i 按 `REQ-TGV2-015` 分离用例名称/标题的当前身份标签依据与其他执行字段的正式事实依据；以公共 validator、真实 MySQL 接收与显式 retry TDD 覆盖合法标签组合、跨功能/测试点越权、角色/环境/阈值/状态/错误码臆造、正式/待确认分离、零部分接收、制品唯一身份、并发单赢家和协调器只重跑失败设计工作
- [x] 6.9j 运行聚焦、真实 MySQL/事务/并发、完整后端、前端、OpenSpec strict、diff/sensitive 与双轴复审；提交推送、构建不可变 JAR、部署唯一 Java 8082 并完成 live 仅 `canRetry` 变化的零副作用门禁后停在夹具 A retry 前
- [ ] 6.10 在 6.9 门禁完成后，用两套实质不同、版本化已审核测试点覆盖非正式三态链路：材料 A 使用具有可执行步骤但仍有待确认条件的独立测试点，按 KEE 实际结果验证 `pending_only` 或 `unable_to_generate` 矩阵，只有实际 `pending_only` 才作为待确认链路证据；材料 B 独立证明 `unable_to_generate`、零用例和非空缺失信息。核对数据库、中文页面、第一张固定 Sheet“需求可测性反馈”和第二张“测试用例”同源且不泄露内部键、原始 JSON、提示词或凭据；不通过临时修改 KEE JSON、猜章节或样本身份特判实现
